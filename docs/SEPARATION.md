# Separation

`evidence-ai-core` is the low-level evidence packet core.

It may later be consumed by:

- `run-lab`
- `trace-lab`
- txtai/paperetl/paperai adapters

It must not import or own those projects.
